# pw-docker

## Rodar local
npm install
npx playwright test

## Rodar com Docker
docker-compose up --build




----------------------------------------------




npm init -y
npm install -D @playwright/test
npx playwright install


-- Rodar Docker
docker-compose up --build



Caso ocorrer erro de versão PW ajustar no arquivo Dockerfile



Após clone do projeto e executar npm install
